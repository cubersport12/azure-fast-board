# Example environment notes for Azure Fast Board
# No secrets belong in this file.

# Optional: force insecure TLS for local testing only
# AZURE_FAST_BOARD_INSECURE_TLS=1

# Dev proxy target for /notifications-api (vite.config.ts)
# Default: http://172.22.91.47:8787
# NOTIFICATIONS_API_PROXY_TARGET=http://172.22.91.47:8787
